console.log(square(5)); // 25
console.log(square(10)); // 100


//Alternatively, you can also use the exponentiation operator (**) 
//to calculate the square of a number:


function square(x) {
  return x ** 2;
}